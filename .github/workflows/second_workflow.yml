name: Workflow for issue event
on:
  issues:
    types: [opened, edited, milestoned, deleted]
jobs:
  info:
    runs-on: ubuntu-latest
      steps:
        - name: Output Github context
          run: echo "${{ toJSON(github) }}" 
